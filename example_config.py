
class BotConfig:
    prefix = "<your-prefix>"
    name = "<bot-name>"
    icon = "<bot-image-path>"
    token = "<your-token-here>"

    db_config = {
        "host": "localhost",
        "user": "root",
        "passwd": "",
        "database": "discord"
    }

    error_rooms = (
        123456789,
    )

    reactionmenu_channels = (
        123456789,
    )
